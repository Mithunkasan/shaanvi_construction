version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: building_contract_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: building_contract
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
